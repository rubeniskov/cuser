{
  "name": "cuser",
  "version": "0.0.1",
  "description": "Distributed user conversation system",
  "private": true,
  "main": "index.js",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs": "./scripts/genreadme > README.md"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ipfs": "^0.52.1",
    "ipfs-api": "^26.1.2"
  },
  "devDependencies": {
    "package-table": "^0.5.5"
  }
}
